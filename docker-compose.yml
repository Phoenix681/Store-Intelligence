version: '3.8'

services:
  api:
    build: .
    container_name: store_intelligence_api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped